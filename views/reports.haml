%h1 Last Reports
%table.datatable.table.table-bordered.table-striped
  %thead
    %tr
      %th Date
      %th Player Name
      %th Otserv
      %th FPS
      %th Graphics Card
      %th Graphics Version
      %th Version
      %th Commit
  %tbody
    - @reports.each do |report|
      %tr.gradeA
        %td= report.date.asctime
        %td=h report.player_name
        %td=h report.otserv_host
        %td= "#{report.fps}/#{report.max_fps}"
        %td=h "#{report.graphics_vendor} #{report.graphics_renderer}"
        %td=h "#{report.graphics_version}"
        %td=h "#{report.build_version} #{report.build_revision}"
        %td=h "#{report.build_commit}"
